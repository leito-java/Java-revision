spring:
  datasource:
    url: jdbc:h2:mem:taskflow-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  cors:
    allowed-origins:
      - http://localhost:4200
